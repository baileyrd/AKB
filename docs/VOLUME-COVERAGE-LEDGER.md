---
id: doc:volume-20:volume-coverage-ledger
title: Twenty-Volume Coverage Ledger
volume: 20
status: partial
model_refs: []
evidence_refs: []
last_verified: 2026-07-30
---

# Twenty-Volume Coverage Ledger

This ledger audits the delivery structure defined in the
[project discussion record](PROJECT-DISCUSSION-RECORD.md). A volume is not
complete merely because a related page exists: its canonical material must
cover the stated subject with scoped evidence, and generated observations must
remain distinguishable from reviewed architecture.

| Volume | Canonical material | Coverage state | Remaining proof/work |
| ---: | --- | --- | --- |
| 1 Executive Architecture | [Ecosystem context](ECOSYSTEM-CONTEXT.md), [domain decomposition](DOMAIN-DECOMPOSITION.md), [eight-layer architecture](EIGHT-LAYER-ARCHITECTURE.md) | `partial` | Both the layer table and the domain table now link each row to its evidence-backed drill-down pages, most substantially for Layers 6/7 (Toolchains, GNU Userland) and their domain-decomposition counterparts, now backed by per-tool/per-library pages across Volumes 5, 6, and 8; Layers 1-3 (Windows/POSIX runtime) remain the shallowest by per-object-page depth, but a 2026-07-30 update corrected the page's stale claim that they lacked controlled observations entirely — each now cites at least one version-qualified local observation, still far short of Layers 6/7's per-object coverage |
| 2 Windows Platform | [Windows platform boundaries](WINDOWS-PLATFORM-BOUNDARIES.md), [NT kernel](WINDOWS-NT-KERNEL-BOUNDARY.md), [Win32 and loader](WINDOWS-WIN32-API-BOUNDARY.md), [console and ConPTY](WINDOWS-CONSOLE-CONPTY-BOUNDARY.md), [filesystem](WINDOWS-FILESYSTEM-BOUNDARY.md), [registry](WINDOWS-REGISTRY-BOUNDARY.md), [security](WINDOWS-SECURITY-BOUNDARY.md), [networking](WINDOWS-NETWORKING-BOUNDARY.md) | `partial` | 8 pages (was 1), one per charter-named subsystem. Deliberately **boundary** pages, not Windows internals — that narrowing is now recorded as [ADR 0001](../charter/adr/0001-windows-platform-contextual-scope.md), which PROJECT-CHARTER.md had made without one, and it means this volume stays thinner than MSYS2's own volumes by design. The WMI-denial gap this row previously described as blocking is **closed**: a 2026-07-30 observation recorded host edition (Windows 11 Home, build 26200), `C:` filesystem type (NTFS), default case-insensitive lookup, and a directly observed symlink-creation privilege requirement — the last of which is the leading explanation for Volume 3's `ln -s` / `test -L` discrepancy. A 2026-07-31 observation closed three more rows: Registry (`HKLM:\SOFTWARE\GitForWindows`), Security (Authenticode verification of `git.exe`, signer confirmed, expired-certificate-but-timestamped-valid nuance recorded), and Networking (no proxy configured). Console/ConPTY is the only row with zero observation, and every finding remains single-host, single-session |
| 3 MSYS Runtime | [msys-2.0.dll](MSYS-2-0-DLL.md), [runtime behavior map](MSYS-RUNTIME-BEHAVIOR-MAP.md), [initialization](MSYS-RUNTIME-INITIALIZATION.md), [Process Manager](MSYS-PROCESS-MANAGER.md), [Signal Manager](MSYS-SIGNAL-MANAGER.md), [Path Conversion](MSYS-PATH-CONVERSION.md), [Mount Manager](MSYS-MOUNT-MANAGER.md), [Filesystem Layer](MSYS-FILESYSTEM-LAYER.md), [PTY and Console](MSYS-PTY-AND-CONSOLE.md), [Environment Manager](MSYS-ENVIRONMENT-MANAGER.md), [POSIX API Surface](MSYS-POSIX-API.md) | `partial` | 11 pages (was 2). The runtime and its eight documented subsystems are modeled as `subsystem:msys2:*` entities and each has a page. Evidence is uneven by design and stated per page. The five bounded 2026-07-30 probes (MSYS 3.6.10) cover process lifecycle, exec, USR1 delivery, symlink classification, and terminal-device presence; a 2026-07-30 comparative run of the same battery against a second, distinct runtime (Git for Windows' bundled `msys-2.0.dll` 3.6.9-b4195d69 vs. 3.6.10) matched on process/exec/signal but diverged materially on the symlink fallback (full-content copy vs. the narrower `test -L` discrepancy) — a concrete demonstration that symlink behavior is installation-dependent, not an MSYS-runtime constant. A 2026-07-31 probe closed the `fork()` emulation gap (`$BASHPID` vs `$$` plus `ps -ef` PPID chaining confirm a real OS-level fork), recorded a partial console/terminal-device observation, and a third installation (`C:\msys64`) reporting the same `uname -a` string as Git for Windows' runtime but a byte-distinct `msys-2.0.dll` also showed a native binary built by one installation failing with a reproducible temp-file error under another's shell. Remaining: path-conversion and mount-table observations (still **none** of any kind), a signal test matrix, PTY/ConPTY allocation and integration tests (the only row here with zero ConPTY-level observation), filesystem probes by host/version, and an exported-symbol and header inventory of the runtime |
| 4 Runtime Environments | [Runtime environments](RUNTIME-ENVIRONMENTS.md), [MSYS](ENVIRONMENT-MSYS.md), [UCRT64](ENVIRONMENT-UCRT64.md), [CLANG64](ENVIRONMENT-CLANG64.md), [CLANGARM64](ENVIRONMENT-CLANGARM64.md), [MINGW64](ENVIRONMENT-MINGW64.md), [MINGW32](ENVIRONMENT-MINGW32.md) | `partial` | 7 pages (was 1). Each of the six environments now has its own page covering all eleven charter attributes: ABI, compiler, runtime, CRT, linker, executable format, package repository, strengths, weaknesses, compatibility, and migration strategy, and a 2026-07-30 update added quantitative per-environment repository package counts from the catalog snapshot (UCRT64 3,898 down to MINGW32 314), consistent with the existing lifecycle labels. Remaining: CLANGARM64 has no controlled runtime observation at all (the observation host was x86_64), so its behavioral sections are reasoned from CLANG64 rather than observed; linker identity is inferred from toolchain family rather than observed per environment; compatibility and migration guidance remains desk-derived rather than exercised; and no MINGW64/MINGW32/CLANGARM64 package is individually modeled |
| 5 GNU Userland | [GNU userland role model](GNU-USERLAND-ROLE-MODEL.md), [GNU Bash](GNU-BASH.md), [GNU Coreutils](GNU-COREUTILS.md), [GNU Grep](GNU-GREP.md), [GNU Sed](GNU-SED.md), [GNU Awk (gawk)](GNU-AWK.md), [GNU Findutils](GNU-FINDUTILS.md), [GNU Tar](GNU-TAR.md), [GNU Gzip](GNU-GZIP.md), [bzip2](BZIP2.md), [XZ Utils](XZ-UTILS.md), [Zstandard](ZSTD.md), [Info-ZIP Zip](INFO-ZIP-ZIP.md), [Info-ZIP UnZip](INFO-ZIP-UNZIP.md), [p7zip](P7ZIP.md), [Lzip](LZIP.md), [GNU Cpio](GNU-CPIO.md), [LZ4](LZ4.md), [GNU Ed](GNU-ED.md), [GNU Nano](GNU-NANO.md), [Vim](VIM.md), [GNU Emacs](GNU-EMACS.md), [less](LESS.md), [mintty](MINTTY.md), [ncurses](NCURSES.md), [OpenSSH](OPENSSH.md), [curl](CURL.md), [Git (MSYS2 package)](GIT-MSYS-PACKAGE.md), [GnuPG](GNUPG.md), [OpenSSL](OPENSSL.md), [GNU Diffutils](GNU-DIFFUTILS.md), [GNU Patch](GNU-PATCH.md) | `partial` | Every component family identified in the role model now has evidence-backed per-tool pages (31 tools total). Diffutils and Patch added 2026-08-02: both had catalog packages but no component entity and no page, despite being how every MSYS2 recipe carries source modifications. Their pages converge on the CRLF question, which `patch(1)` addresses for Windows directly and which this knowledge base cannot close because the MSYS mount table is uncaptured. gnu.org returned 403 through this environment's proxy, so both manual pages were read from man7.org's mirror and the evidence records say so. A 2026-07-30 audit found and fixed two Dependencies-count errors: Git (MSYS2 package) stated "eleven," the catalog and the page's own table both show fourteen; GnuPG stated "fourteen," actual sixteen. Remaining work is depth — per-tool controlled observations to replace documentation-only evidence, plus any additional tools discovered beyond the currently identified families |
| 6 Libraries | [Libraries architecture](LIBRARIES-ARCHITECTURE.md), [library family classification](LIBRARY-FAMILY-CLASSIFICATION.md), [header and metadata indexes](HEADER-AND-METADATA-INDEXES.md), [libstdc++](LIBSTDCXX.md), [libc++](LIBCXX.md), [zlib](ZLIB.md), [GNU libiconv](GNU-LIBICONV.md), [GNU gettext](GNU-GETTEXT.md), [Expat](EXPAT.md), [libxml2](LIBXML2.md), [ICU](ICU.md), [Boost](BOOST.md), [SQLite](SQLITE3.md), [GNU Readline](GNU-READLINE.md), [GNU MP](GNU-GMP.md), [GNU MPFR](GNU-MPFR.md), [GNU MPC](GNU-MPC.md), [isl](LIBISL.md), [libwinpthread](LIBWINPTHREAD.md), [winpthreads](WINPTHREADS.md), [PCRE2](PCRE2.md), [libgpg-error](LIBGPG-ERROR.md), [libgcrypt](LIBGCRYPT.md), [libassuan](LIBASSUAN.md), [libksba](LIBKSBA.md), [nPth](NPTH.md), [Nettle](NETTLE.md), [GnuTLS](GNUTLS.md), [GNU libidn2](GNU-LIBIDN2.md), [GNU Libtasn1](GNU-LIBTASN1.md), [p11-kit](P11-KIT.md), [libpsl](LIBPSL.md), [GNU libunistring](GNU-LIBUNISTRING.md), [libnghttp2](LIBNGHTTP2.md), [libnghttp3](LIBNGHTTP3.md), [libngtcp2](LIBNGTCP2.md), [libedit](LIBEDIT.md), [libxcrypt](LIBXCRYPT.md), [libfido2](LIBFIDO2.md), [Heimdal](HEIMDAL.md), [cppdap](CPPDAP.md), [JsonCpp](JSONCPP.md), [libarchive](LIBARCHIVE.md), [libuv](LIBUV.md), [RHash](RHASH.md), [file](FILE.md), [GNU termcap](GNU-TERMCAP.md), [WinEditLine](WINEDITLINE.md), [libgcrypt (MSYS)](LIBGCRYPT-MSYS.md), [libassuan (MSYS)](LIBASSUAN-MSYS.md), [libksba (MSYS)](LIBKSBA-MSYS.md), [nPth (MSYS)](NPTH-MSYS.md), [Nettle (MSYS)](NETTLE-MSYS.md), [libgpg-error (MSYS)](LIBGPG-ERROR-MSYS.md), [GNU libintl](GNU-LIBINTL.md), [libcurl](LIBCURL.md), [PCRE2 (MSYS)](PCRE2-MSYS.md), [PCRE (MSYS)](PCRE-MSYS.md), [GNU Readline (MSYS)](GNU-READLINE-MSYS.md), [GNU Libltdl](GNU-LIBLTDL.md), [Zstandard (library)](LIBZSTD.md), [LLVM libraries](LLVM-LIBS.md), [Clang libraries](CLANG-LIBS.md), [xxHash](XXHASH.md), [liblzma](LIBLZMA.md), [GNU libiconv (MSYS)](GNU-LIBICONV-MSYS.md), [GNU MP (MSYS)](GNU-GMP-MSYS.md), [Expat (MSYS)](EXPAT-MSYS.md), [libxml2 (MSYS)](LIBXML2-MSYS.md), [zlib (CLANG64)](ZLIB-CLANG64.md), [Zstandard (CLANG64)](LIBZSTD-CLANG64.md), [libcbor](LIBCBOR.md), [Heimdal runtime libraries](HEIMDAL-LIBS.md), [zlib (MSYS)](ZLIB-MSYS.md), [Brotli](BROTLI.md), [ca-certificates](CA-CERTIFICATES.md), [libssh2](LIBSSH2.md), [Zstandard (MSYS library)](LIBZSTD-MSYS.md), [libbz2](LIBBZ2.md), [GNU MPFR (MSYS)](GNU-MPFR-MSYS.md), [libnettle (MSYS)](LIBNETTLE-MSYS.md), [Hogweed (MSYS)](LIBHOGWEED-MSYS.md), [libopenssl](LIBOPENSSL.md), [ncurses (UCRT64)](NCURSES-UCRT64.md), [libxml2 (CLANG64)](LIBXML2-CLANG64.md), [liblzma (CLANG64)](LIBLZMA-CLANG64.md), [winpthreads (CLANG64)](WINPTHREADS-CLANG64.md), [libwinpthread (CLANG64)](LIBWINPTHREAD-CLANG64.md), [libffi (MSYS)](LIBFFI-MSYS.md), [libffi (CLANG64)](LIBFFI-CLANG64.md), [curl (UCRT64)](CURL-UCRT64.md), [OpenSSL (UCRT64)](OPENSSL-UCRT64.md), [Brotli (UCRT64)](BROTLI-UCRT64.md), [libssh2 (UCRT64)](LIBSSH2-UCRT64.md), [c-ares (UCRT64)](C-ARES-UCRT64.md), [GNU libunistring (UCRT64)](GNU-LIBUNISTRING-UCRT64.md), [GNU libidn2 (UCRT64)](GNU-LIBIDN2-UCRT64.md), [libpsl (UCRT64)](LIBPSL-UCRT64.md), [ca-certificates (UCRT64)](CA-CERTIFICATES-UCRT64.md), [libnghttp2 (UCRT64)](LIBNGHTTP2-UCRT64.md), [libngtcp2 (UCRT64)](LIBNGTCP2-UCRT64.md), [libnghttp3 (UCRT64)](LIBNGHTTP3-UCRT64.md), [libffi (UCRT64)](LIBFFI-UCRT64.md), [GNU Libtasn1 (UCRT64)](GNU-LIBTASN1-UCRT64.md), [p11-kit (UCRT64)](P11-KIT-UCRT64.md), [GnuTLS (UCRT64)](GNUTLS-UCRT64.md), [liblzma (MSYS)](LIBLZMA-MSYS.md), [liblz4 (MSYS)](LIBLZ4-MSYS.md), [libarchive (MSYS)](LIBARCHIVE-MSYS.md), [xxHash (MSYS)](XXHASH-MSYS.md), [libsqlite (MSYS)](LIBSQLITE-MSYS.md), [libsasl (MSYS)](LIBSASL-MSYS.md), [popt (MSYS)](POPT-MSYS.md), [LZO (MSYS)](LIBLZO2-MSYS.md), [APR](APR-MSYS.md), [APR-util](APR-UTIL-MSYS.md), [Serf](LIBSERF-MSYS.md), [GPGME (MSYS)](LIBGPGME-MSYS.md), [GMP (CLANG64)](GNU-GMP-CLANG64.md), [GNU MPFR (CLANG64)](GNU-MPFR-CLANG64.md), [Nettle (CLANG64)](NETTLE-CLANG64.md), [isl (CLANG64)](LIBISL-CLANG64.md), [GNU MPC (CLANG64)](GNU-MPC-CLANG64.md), [Brotli (CLANG64)](BROTLI-CLANG64.md), [c-ares (CLANG64)](C-ARES-CLANG64.md), [GNU libiconv (CLANG64)](GNU-LIBICONV-CLANG64.md), [GNU gettext (CLANG64)](GNU-GETTEXT-CLANG64.md), [GNU Libtasn1 (CLANG64)](GNU-LIBTASN1-CLANG64.md), [p11-kit (CLANG64)](P11-KIT-CLANG64.md), [ca-certificates (CLANG64)](CA-CERTIFICATES-CLANG64.md), [GNU libunistring (CLANG64)](GNU-LIBUNISTRING-CLANG64.md), [GNU libidn2 (CLANG64)](GNU-LIBIDN2-CLANG64.md), [libpsl (CLANG64)](LIBPSL-CLANG64.md), [libgpg-error (CLANG64)](LIBGPG-ERROR-CLANG64.md), [libgcrypt (CLANG64)](LIBGCRYPT-CLANG64.md), [libassuan (CLANG64)](LIBASSUAN-CLANG64.md), [libksba (CLANG64)](LIBKSBA-CLANG64.md), [OpenSSL (CLANG64)](OPENSSL-CLANG64.md), [libssh2 (CLANG64)](LIBSSH2-CLANG64.md), [bzip2 (CLANG64)](BZIP2-CLANG64.md), [BLAKE2 (libb2) (CLANG64)](LIBB2-CLANG64.md), [LZ4 (CLANG64)](LZ4-CLANG64.md), [WinEditLine (CLANG64)](WINEDITLINE-CLANG64.md), [Expat (CLANG64)](EXPAT-CLANG64.md), [PCRE2 (CLANG64)](PCRE2-CLANG64.md), [libarchive (CLANG64)](LIBARCHIVE-CLANG64.md), [libnghttp2 (CLANG64)](LIBNGHTTP2-CLANG64.md), [libnghttp3 (CLANG64)](LIBNGHTTP3-CLANG64.md), [GnuTLS (CLANG64)](GNUTLS-CLANG64.md), [libngtcp2 (CLANG64)](LIBNGTCP2-CLANG64.md), [curl (CLANG64)](CURL-CLANG64.md), [bzip2 (UCRT64)](BZIP2-UCRT64.md), [LZ4 (UCRT64)](LZ4-UCRT64.md), [ncurses (CLANG64)](NCURSES-CLANG64.md), [libcbor (UCRT64)](LIBCBOR-UCRT64.md), [libcbor (CLANG64)](LIBCBOR-CLANG64.md), [libfido2 (UCRT64)](LIBFIDO2-UCRT64.md), [libfido2 (CLANG64)](LIBFIDO2-CLANG64.md), [BLAKE2 (libb2) (UCRT64)](LIBB2-UCRT64.md), [category — graphics](LIBRARY-CATEGORY-GRAPHICS.md), [GUI](LIBRARY-CATEGORY-GUI.md), [audio](LIBRARY-CATEGORY-AUDIO.md), [video](LIBRARY-CATEGORY-VIDEO.md), [imaging](LIBRARY-CATEGORY-IMAGING.md), [logging](LIBRARY-CATEGORY-LOGGING.md), [testing](LIBRARY-CATEGORY-TESTING.md) | `partial` | **Seven category pages added 2026-08-02**, closing the last Increment 5 authoring item. They rank each category from the catalog snapshot by dependents summed across environment variants — the correction that matters, since `generated/library-candidates.md` counted variants separately and so ranked `libpng` tenth when it is fourth. All seven were then recomputed against build-time edges: the testing page carried the sharpest finding in this volume, ten test frameworks across roughly 47 packages recording **one** dependent in total, because `model/catalog/current.json` carried no build-time or check-time dependency edges at all; both a repository-database and a PKGBUILD-recipe importer now exist, and `python-pytest` alone records 1,328. spdlog's upstream page was unreachable (github.com returned 403 through this environment's proxy), so the logging page is the only one of the seven without a verified upstream citation and says so. One hundred and fifty-seven per-library pages exist; OpenSSL (CLANG64) unblocked libssh2 (CLANG64), then five more zero-dependency bases (bzip2, BLAKE2/libb2 — the first BLAKE2-family entity in this knowledge base, LZ4, WinEditLine, Expat) plus PCRE2 fed into libarchive (CLANG64) closing all ten of its own catalog dependencies in one pass; a final batch closed curl (CLANG64) itself — libnghttp2, libnghttp3, GnuTLS (11/11), and libngtcp2 (2/2) — completing the CLANG64 network-transfer library cluster entirely and mirroring curl (UCRT64)/libcurl (MSYS)'s own earlier full-coverage milestones; a second CLANG64 chain (libunistring → libidn2 → libpsl) reused the libiconv/gettext-runtime base from the ca-certificates chain; a third chain closed the GnuPG crypto-stack (libgpg-error → libgcrypt/libassuan/libksba) for CLANG64; a five-entity ca-certificates (CLANG64) chain (libiconv → gettext-runtime/libtasn1 → p11-kit → ca-certificates) was scoped then built in dependency order; a post-batch audit retroactively closed two more edges this unlocked (libxml2 and liblzma, both CLANG64); GNU MPFR, Nettle, and isl (all CLANG64) each closed with full 1/1 coverage alongside GMP (CLANG64), and GNU MPC (CLANG64) with full 2/2; Brotli and c-ares (CLANG64) were added as zero-dependency siblings, the latter correcting a now-stale "no CLANG64 sibling" claim; a three-page APR/APR-util/Serf chain closed Subversion's remaining uncited MSYS dependencies (Subversion itself remains unmodeled and is flagged on all three); a full citation sweep closed GPGME (MSYS) at 3/3; a 2026-08-02 batch closed two more citation gaps (GnuPG and Heimdal had each cited `package:msys2:libsqlite` by name without a graph edge) and added libsasl (MSYS) and popt (MSYS); curl (UCRT64) reached full 12/12 dependency coverage, the second package after libcurl (MSYS) to do so; a 2026-07-30 correction fixed five GnuPG requires-edges pointing at UCRT64 entities instead of the MSYS siblings; liblz4 (MSYS) and libarchive (MSYS) were added as distinct catalog entities, the latter closing eight non-boilerplate edges in one pass; a correction batch fixed a false "no dependencies" claim on ca-certificates (MSYS) and formalized two citation-only gaps (libedit→ncurses, libfido2→openssl); follow-on batches closed bzip2 (UCRT64), LZ4 (UCRT64), ncurses (CLANG64), the libfido2 sibling triple via a new libcbor (UCRT64)/(CLANG64) pair, and libarchive's final declined edge BLAKE2 (libb2) (UCRT64), reaching 10/10. A parallel 2026-07-30 correction sweep scoped this volume's superlatives and fixed eleven numeric drifts: zlib is the most-depended-upon package *among components and libraries this knowledge base actually models* at 299 reverse dependents (libwinpthread third at 152, GNU libintl the most widely-depended-upon MSYS-only library at 59) — undocumented packages such as Python at 965 exceed all of them in the full catalog view, and the same unscoped wording was fixed in `LIBSTDCXX.md`, `LIBRARIES-ARCHITECTURE.md`, and `LIBWINPTHREAD.md`; GNU libiconv's reverse-dependency count was corrected from 82 to 81; ca-certificates' claimed zero `runtime-depends-on` edges corrected to six; and winpthreads (CLANG64), ca-certificates (UCRT64), Clang libraries, liblzma (UCRT64 and CLANG64), libxml2 (CLANG64), ncurses (UCRT64), p11-kit (UCRT64), and zlib (CLANG64) each had counts corrected against the raw catalog CSV. Two libraries now carry the full family-classification methodology (headers, pkg-config/CMake metadata, static/import library, runtime DLL, each a typed graph entity): zlib as the 2026-07-30 worked example, zstd (UCRT64) on 2026-07-31 reusing Volume 11's archive. The remaining per-library pages are scoped to package/dependency-level evidence only, along with observed family-level package/artifact coverage beyond the bounded installation |
| 7 Package Management | [Pacman architecture](PACMAN-ARCHITECTURE.md), [repository trust model](PACMAN-REPOSITORY-TRUST-MODEL.md), [transaction model](PACMAN-TRANSACTIONS.md), [database model](PACMAN-DATABASE-MODEL.md), [repository layout](PACMAN-REPOSITORY-LAYOUT.md), [package signing](PACMAN-PACKAGE-SIGNING.md), [hooks and cache](PACMAN-HOOKS-AND-CACHE.md) | `partial` | 7 pages (was 2), and pacman is modeled as `package-manager:archlinux:pacman` — it previously had no entity. Mechanism is grounded in the pacman(8), pacman.conf(5), and alpm-hooks(5) manual pages, with a standing caveat on every page that those are Arch's manual pages while MSYS2 places its root, database, cache, and hook directories under the MSYS2 installation. The trust model's boundary table is now substantially executed rather than wholly unexecuted: a 2026-07-30 update cited the already-collected per-repository sync-database SHA-256 hashes (six repositories) for the Sync databases row; a 2026-07-31 update verified a downloaded zstd (UCRT64) archive's detached `.sig` as a genuine `gpg`-good signature from a named MSYS2 packager's key, explicitly *not* through pacman's own keyring-trust chain; and a 2026-07-31 install of a new MSYS2 environment (`C:\msys64`) closed the Transaction and Hooks/cache rows with two fully logged ALPM transactions (17-package `gcc`, 1-package `make`), a real `texinfo-install.hook` firing, a 36-file/71.68 MiB cache, `pacman -Qi`'s own `Validated By: Signature` confirming the gpg finding as pacman's recorded outcome, and `pacman_version` captured directly (`6.1.0`) after the collector had never captured it. Remaining: repair observations, and an effective `pacman.conf` and hook inventory for the *tracked* snapshot, which predates this installation |
| 8 Toolchains | [Toolchain role model](TOOLCHAIN-ROLE-MODEL.md), [GCC](GNU-GCC.md), [GNU Binutils](GNU-BINUTILS.md), [GDB](GNU-GDB.md), [Clang](CLANG.md), [LLD](LLD.md), [LLDB](LLDB.md), [CMake](CMAKE.md), [Meson](MESON.md), [Ninja](NINJA.md), [pkgconf](PKGCONF.md), [GNU Autoconf](GNU-AUTOCONF.md), [GNU Automake](GNU-AUTOMAKE.md), [GNU Libtool](GNU-LIBTOOL.md), [GNU Make](GNU-MAKE.md), [LLVM](LLVM.md) | `partial` | Every toolchain-tool group and role-model row identified for this volume now has evidence-backed per-tool pages (15 tools, plus the C++ library row resolved via Volume 6's libstdc++/libc++ pages); a 2026-07-30 audit found and fixed two Dependencies-count errors: GDB stated "eleven," the catalog and the page's own table both show twelve plus a thirteenth optional edge; GCC stated "eleven," actual twelve. The LLVM umbrella page was added 2026-08-02: Clang, LLD, and LLDB each had a page while the infrastructure they are built on had none. Its measured content is the MSYS2 packaging split — `llvm` as a metapackage pinning `llvm-libs` and `llvm-tools`, and the MSYS and native sides on different major versions. What the `llvm-tools` package installs is listed from upstream rather than observed, because no package file manifest has been collected; observed build outputs beyond the existing bounded compile/execute probe also remain |
| 9 Git for Windows | [Distribution boundary](GIT-FOR-WINDOWS-BOUNDARY.md), [launcher/startup](GIT-FOR-WINDOWS-LAUNCHER-STARTUP.md), [transport boundaries](GIT-FOR-WINDOWS-TRANSPORT-BOUNDARIES.md), [package/source mappings](GIT-FOR-WINDOWS-PACKAGE-SOURCE-MAPPINGS.md), [Git Bash and MSYS interaction](GIT-FOR-WINDOWS-GIT-BASH.md), [credential manager](GIT-FOR-WINDOWS-CREDENTIAL-MANAGER.md), [HTTP transport](GIT-FOR-WINDOWS-HTTP-TRANSPORT.md), [DLL loading](GIT-FOR-WINDOWS-DLL-LOADING.md) | `partial` | 8 pages (was 4). The three charter terms that appeared in zero pages — MSYS interaction, HTTP transport, credential manager — each have one, grounded in gitforwindows.org, gitcredentials(7), and git-config. The HTTP page records the openssl/schannel trust-store divergence, the distribution's most consequential Windows-specific behavior. The "no controlled observation of any Git for Windows installation" gap this row previously stated is **closed**: a 2026-07-30 local observation added version-qualified launcher-artifact hashes, a byte-level PE-import parse of `git.exe` (10 imported DLLs, 4 MSYS-derived), and a shell-context comparison showing `ssh`/`curl` resolve to genuinely different binaries depending on PATH ordering — correcting the prior claim that they always resolve outside the Git for Windows tree; a 2026-07-31 third-context observation found `cmd.exe` resolves identically to PowerShell (both share a base `PATH` lacking `Git\mingw64\bin`), explaining Git Bash's distinct resolution as its own shell-launch `PATH` augmentation. Remaining: Git Credential Manager's repository was unreachable from the collection environment so its internals are unverified, and all DLL/transport evidence is from one workstation and one `PATH` configuration |
| 10 Interactive Architecture Explorer | [Explorer hierarchy](DIAGRAM-HIERARCHY.md), generated explorer routes | `partial` | A 2026-07-30 run of `tests.test_explorer`'s large-graph performance test (2,000 objects/4,000 edges) measured ~0.1s, two orders of magnitude under its 10s bound, and the HTML-escaping security verification confirmed the generator produces populated, correctly-escaped per-object views for arbitrary entity data. A 2026-07-31 update added a real interactive, canvas-rendered force-directed graph view (vendored D3 v7, `#/graph/<view>` bounded per existing named view, `#/graph-node/<id>` seed-and-expand neighborhood mode, both capped at 800 nodes against the ~16,000-entity full graph), closing this volume's zoomable-exploration gap. A second implementation was built independently on this branch and withdrawn in favor of it — one implementation is better than two. Remaining: the graph view is verified by JS syntax validation and tests asserting the vendored file and real D3 API calls in the output, but has not been visually confirmed in a live browser session, since no connected browser tooling is available in this authoring environment; demonstrated populated deep-object views and prose depth also remain, and this volume still has the lowest words-per-page figure in the knowledge base |
| 11 Package Catalog | [Repository package inventory](REPOSITORY-PACKAGE-INVENTORY.md), [package file inventory](PACKAGE-FILE-INVENTORY.md), [binary graph](BINARY-DLL-DEPENDENCY-GRAPH.md), [the deep-inventory blocker](DEEP-INVENTORY-BLOCKER.md) | `partial` | Direct artifact collection now covers three archive payloads beyond the bounded installation, each hash-recorded: zlib (UCRT64) and curl (MSYS) on 2026-07-30 — curl's byte-level PE imports confirmed two of its catalog-declared dependency edges directly — and zstd (UCRT64) on 2026-07-31, whose `zstd.exe` and `pzstd.exe` both link the package's own `libzstd.dll` at the byte level. That is three packages out of 15,711, far short of collection at catalog scale. The nine unchecked roadmap items behind this volume's depth are tracked in [the blocker page](DEEP-INVENTORY-BLOCKER.md), which records what a deep-inventory run over the catalog would need and which statements elsewhere are qualified by its absence |
| 12 Source Code Organization | [Source code organization](SOURCE-CODE-ORGANIZATION.md), recipe-tree collectors, and [Git for Windows package/source mappings](GIT-FOR-WINDOWS-PACKAGE-SOURCE-MAPPINGS.md) | `partial` | This row's own description had gone stale: `SOURCE-CODE-ORGANIZATION.md` already carries a "Bounded provenance slice: zlib" section (2026-07-30) matching a retrieved upstream tarball's SHA-256 against the recipe's declared digest, confirming both recipe-local patches' digests, and recording a controlled build that applied both patches but failed under MSYS GCC 15.3.0 on `gzlib.c`; still genuinely open: byte identity between the retrieved source and the installed DLL, a successful build past that failure, and any package beyond zlib |
| 13 Dependency Analysis | [Reverse dependency impact analysis](REVERSE-DEPENDENCY-IMPACT-ANALYSIS.md) | `partial` | A 2026-07-30 update linked this page to its two already-existing but previously uncited generated views (`reverse-dependency-impact.json`, `binary-dependency-graph.json`/`binary-dependency-report.md`) and added a worked example on zlib showing three correctly different reverse-dependent counts (299 all-edges, 297 runtime-only, 34 byte-level PE importers on the bounded installation) across edge families; a 2026-07-31 controlled exercise found the Recipe build/check dependency edge family's import code already works but resolved zero edges against a real recipe (zstd) because its dependency names are unexpanded `${MINGW_PACKAGE_PREFIX}` templates that the deliberately-non-executing parser cannot resolve — the root cause, not a missing feature; the Metadata requirement edge family still has no generated view, and broader resolved binary/metadata dependency observations beyond the bounded installation remain open |
| 14 Build Systems | [Build-system role model](BUILD-SYSTEM-ROLE-MODEL.md), [artifact flow mappings](BUILD-ARTIFACT-FLOW-MAPPINGS.md) | `partial` | A 2026-07-30 update cross-linked the already-recorded zlib build attempt (source retrieval and patch verification succeeded; compilation failed on `gzlib.c` under GCC 15.3.0) as this page's first worked example, mapped explicitly onto its stage table; the pipeline was not reached past Compile and link; a 2026-07-31 second, independently-scoped exercise (a real winget-installed MSYS2 + UCRT64 GCC 16.1.0 toolchain, upstream zlib-1.3.2.tar.gz, zlib's own win32/Makefile.gcc, no recipe patches) reached compile, link, AND execution for the first time in this knowledge base -- gzlib.c compiled cleanly under the newer GCC, the full library/DLL/test-binary set built, and running example.exe surfaced a genuine gzseek functional-test failure (three other checks passed) -- also finding and root-causing a reproducible cross-MSYS-runtime build failure when the new toolchain is invoked from Git for Windows' distinct msys-2.0.dll runtime instead of its own; install-staging/package-archive/installed-file stages and a source-to-installed-DLL reproducibility comparison remain unexercised for every documented package |
| 15 Extension and Plugin Architecture | [Extension and plugin architecture](EXTENSION-AND-PLUGIN-ARCHITECTURE.md) | `partial` | Concrete implementations are now cited for every extension point except external plugin (none exists yet); a 2026-07-30 exercise called the real importer functions with a synthetic incompatible-`schema_version` manifest and confirmed both rejected it while accepting the matching version, proving the rejection guard works; a 2026-07-31 update exercised the real forward-migration case for the first time: runtime-observation.schema.json's schema_version was bumped from a pinned const `1.0.0` to `enum: ["1.0.0", "1.1.0"]`, formally declaring the `probes`/`behavior_probes` fields the collector had always emitted but the old schema never listed, and a new `migrate_to_current_schema()` transforms a 1.0.0 document to 1.1.0 without loss, verified by 4 new unit tests and a real CLI run against a synthetic fixture; this exercises one schema/importer pair only, `model/graph.json`'s own `schema_version` and the deep-inventory schema have never been bumped |
| 16 Security | [Threat model and supply chain](THREAT-MODEL-AND-SUPPLY-CHAIN.md) | `partial` | A 2026-07-30 verification exercised the Explorer row's "HTML escaping and static generation" control directly against `tools/build_explorer.py`'s real `build()` function using synthetic XSS-shaped test entities (never staged in this repository) and confirmed both the Python-side `html.escape` path and the generated page's own client-side `esc()` function neutralize the payloads; a 2026-07-31 code inspection verified the Package recipes row's "Static parsing only; PKGBUILDs never execute" control by confirming no exec/eval/subprocess/os.system call touches PKGBUILD content in `parse_pkgbuild()`, `collect_recipe_tree.py`, or `import_recipe_tree.py`; a second 2026-07-31 inspection of the Generated projections row found its "atomic current-view replacement" wording overclaimed what the code does — validate-before-write is real (a failed import never touches `CURRENT`), but the `CURRENT` file write itself is a direct write, not an `os.replace`-based swap, corrected in the row and page rather than left as a false green check; the remaining five threat-model rows still have no measured verification, only documented controls |
| 17 Performance | [Ecosystem performance architecture](ECOSYSTEM-PERFORMANCE-ARCHITECTURE.md) | `inferred` | Rebuilt 2026-08-02. The volume previously held only `PERFORMANCE-EXPERIMENTS.md`, which benchmarks this repository's own tooling; that page is now Volume 20, where its 2026-07-30 and 2026-07-31 same-host benchmark samples live. The four hot paths the charter names — fork emulation, path translation, mount-table lookup, pacman transaction cost — are analysed from the published upstream algorithms plus this repository's observed catalog shape. **No MSYS2 timing measurement exists anywhere in this knowledge base**: the controlled observations recorded in Volumes 3, 7, 14, and 19 establish behavior, not cost, and none of them timed anything. The page states that in its first paragraph rather than implying measurement it does not have |
| 18 Developer Guide | [Developer guide](DEVELOPER-GUIDE.md), [building](DEVELOPER-BUILDING-SOFTWARE.md), [debugging](DEVELOPER-DEBUGGING.md), [packaging](DEVELOPER-PACKAGING.md), [migration](DEVELOPER-MIGRATION.md), [repository workflow](DEVELOPER-WORKFLOW.md) | `inferred` | Authored 2026-08-02. The volume's charter question — select, build, debug, package, migrate — is now answered by five pages grounded in MSYS2's own wiki and docs rather than in Arch or Cygwin inheritance, alongside the `DEVELOPER-WORKFLOW.md` page a 2026-07-30 change split out of combined operator/refresh content that had been mistagged `volume: 20`. Observed exercise is uneven: the repository-workflow page cites PR #123 as a genuine step-by-step run including honestly-recorded deviations, and a 2026-07-30 zlib build attempt reached compile, link, and execution on a real MSYS2 installation, but **no command on the five MSYS2-facing pages has itself been executed** and each page says so in its closing section. MSYS2's effective `makepkg.conf` is uncaptured, so no claim is made about default build flags, integrity algorithms, or compression; a build/generator-code change, a failing-CI recovery path, and a multi-committer review cycle also remain unexercised |
| 19 Operations Guide | [Upgrade/rollback/repair/migration](UPGRADE-ROLLBACK-REPAIR-MIGRATION.md), [Operator refresh workflow](OPERATOR-REFRESH-WORKFLOW.md) | `partial` | The same 2026-07-30 split gave Volume 19 a second, correctly-tagged canonical page (operator refresh, scheduled operation, failure/escalation), previously undiscoverable from this row; a 2026-07-31 real attempt against a genuinely new MSYS2 installation (`C:\msys64`) found `Update-Akb.ps1`'s catalog-collection step hits a reproducible, harness-specific parameter-binding error on the full ~15,700-package `pacman -Si` dump (confirmed unrelated to the data itself), but the runtime-observation collect-and-import sub-step succeeded completely when run independently, producing a genuine imported snapshot; deep-inventory collection, a full unblocked refresh run, and a repair drill remain open |
| 20 Reference Appendices | [Terminology](TERMINOLOGY-AND-BOUNDARIES.md), [requirements traceability](REQUIREMENTS-TRACEABILITY.md), [AKB tooling benchmarks](PERFORMANCE-EXPERIMENTS.md), local-retention and evidence documentation | `partial` | Requirement-by-requirement completion evidence. Gained the tooling-benchmark page on 2026-08-02, re-filed out of Volume 17 where it had been standing in for ecosystem performance analysis that did not exist. A 2026-07-30 refresh corrected stale figures in the requirements traceability matrix (claim/evidence counts had drifted to roughly a third of the then-current totals; the Volume 6 row still said "first five foundational libraries" against a now-157-page reality) and named the volumes still lacking per-object citations; completion evidence for the still-`Partial` rows remains open |

## Current evidence boundary

The local-only evidence includes full repository-file ownership plus a
hash-verified expanded installed-artifact snapshot across the isolated MSYS2
installation. Its standalone typed overlay contains 48,299 entities and
54,993 relationships; it remains separate from the multi-million-object
ownership projection to stay within workstation-safe memory limits. It does
not prove universal runtime behavior, source-to-payload byte identity, or
complete artifact coverage for every repository package. Those distinctions
are intentional and remain completion gates for the relevant rows above.

## Completion rule

Do not mark this ledger complete until every row has a canonical page or
generated view, direct evidence appropriate to its stated scope, and a
reproducible validation path. Cross-links alone do not close a gap.

<!-- BEGIN GENERATED volume-metrics -->

## Measured Coverage

Page, heading, word, and reference counts are measured from the pages themselves by `tools/build_volume_ledger.py`. Generated blocks are excluded from the prose counts, so a volume cannot inflate its word count by carrying more diagrams.

The coverage state is a judgment, not a measurement. It is authored in [`model/volume-coverage.json`](../model/volume-coverage.json) with a rationale per volume, so changing it shows up in a diff.

| Vol | Title | State | Pages | `##` | Words | Words/page | Evidence | Model refs |
| ---: | --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | Executive Architecture | `partial` | 5 | 13 | 1,459 | 292 | 1 | 7 |
| 2 | Windows Platform | `partial` | 8 | 54 | 3,406 | 426 | 4 | 5 |
| 3 | MSYS Runtime | `partial` | 11 | 147 | 6,459 | 587 | 3 | 12 |
| 4 | Runtime Environments | `partial` | 8 | 106 | 5,545 | 693 | 2 | 15 |
| 5 | GNU Userland | `partial` | 32 | 459 | 23,920 | 748 | 33 | 98 |
| 6 | Libraries | `partial` | 164 | 2395 | 114,472 | 698 | 83 | 358 |
| 7 | Package Management | `partial` | 7 | 84 | 4,409 | 630 | 6 | 3 |
| 8 | Toolchains | `partial` | 16 | 223 | 13,484 | 843 | 22 | 66 |
| 9 | Git for Windows | `partial` | 8 | 74 | 4,576 | 572 | 8 | 5 |
| 10 | Interactive Architecture Explorer | `partial` | 8 | 14 | 2,190 | 274 | 1 | 4 |
| 11 | Package Catalog | `partial` | 9 | 31 | 5,233 | 581 | 5 | 16 |
| 12 | Source Code Organization | `partial` | 2 | 6 | 1,063 | 532 | 2 | 3 |
| 13 | Dependency Analysis | `partial` | 1 | 5 | 1,007 | 1,007 | 3 | 3 |
| 14 | Build Systems | `partial` | 2 | 7 | 1,486 | 743 | 1 | 10 |
| 15 | Extension and Plugin Architecture | `partial` | 1 | 5 | 966 | 966 | 1 | 0 |
| 16 | Security | `partial` | 1 | 7 | 1,271 | 1,271 | 2 | 0 |
| 17 | Performance | `inferred` | 1 | 8 | 2,258 | 2,258 | 5 | 7 |
| 18 | Developer Guide | `inferred` | 6 | 45 | 5,644 | 941 | 14 | 10 |
| 19 | Operations Guide | `partial` | 4 | 23 | 2,297 | 574 | 1 | 0 |
| 20 | Reference Appendices | `partial` | 8 | 44 | 15,557 | 1,945 | 2 | 0 |
| | **Total** | | **302** | **3,750** | **216,702** | | | |

### What the numbers say

- Volume 6 (Libraries) holds 164 of 302 pages and 52.8% of all prose.
- Volume 10 (Interactive Architecture Explorer) has the lowest prose density at 274 words per page.
- 2 of the charter's 8 coverage states are in use. Unused: `unknown`, `not-applicable`, `planned`, `verified`, `deprecated`, `superseded`.

### Coverage states

| State | Meaning |
| --- | --- |
| `unknown` | The volume's coverage has not been assessed. |
| `not-applicable` | The volume does not apply to this knowledge base's scope. |
| `planned` | The subject is identified and named but not yet addressed. Pages may exist as stubs. |
| `partial` | The subject is addressed, with stated gaps. The default state for work in progress. |
| `verified` | Every load-bearing claim rests on observation of the thing described. |
| `inferred` | The material is sound but reasoned from documentation rather than observed. Distinct from partial: the gap is in evidence class, not in coverage extent. |
| `deprecated` | Superseded material retained for resolvability. |
| `superseded` | Replaced by another volume; retained as a pointer. |

### Why each volume carries the state it does

**1 Executive Architecture — `partial`.** Five pages carrying the L0/L1/L2 baseline. Cites no evidence record on any page, so the layer and domain models are asserted rather than sourced.

**2 Windows Platform — `partial`.** Eight boundary pages, scope narrowed deliberately by ADR 0001. The WMI-denial gap that made this volume documentation-only is closed: a 2026-07-30 observation recorded host edition, NTFS filesystem type, case-insensitivity, and a symlink-creation privilege requirement, and a 2026-07-31 observation closed the Registry, Security, and Networking rows. Console/ConPTY is the only row with no observation, and every finding is single-host, single-session.

**3 MSYS Runtime — `partial`.** Eleven pages, one per documented subsystem. Probes across three distinct MSYS runtimes cover process lifecycle, exec, signal delivery, fork emulation, symlink classification, and terminal-device presence, and show symlink behavior to be installation-dependent rather than a runtime constant. Path conversion, the mount table, and environment conversion still have no observation of any kind.

**4 Runtime Environments — `partial`.** All six environments covered against the charter's eleven attributes. CLANGARM64 has no runtime observation at all - the observation host was x86_64 - so its behavioral sections are reasoned from CLANG64.

**5 GNU Userland — `partial`.** Thirty-one per-tool pages covering every family in the role model. Evidence is upstream documentation throughout; no tool page rests on a controlled run of the tool it describes.

**6 Libraries — `partial`.** The largest volume by an order of magnitude. Per-library pages plus seven category pages, now ranked on runtime and build-time edges alike. Two libraries — zlib and zstd (UCRT64) — carry full family classification from package-archive analysis; the rest are scoped to package and dependency evidence, because the deep-inventory pipeline has run against 2 of 15,711 packages.

**7 Package Management — `partial`.** Mechanism is grounded in Arch's pacman, pacman.conf, and alpm-hooks manual pages, and every page carries that caveat. A 2026-07-31 MSYS2 installation supplied real evidence for the transaction, hook, cache, and signature-validation rows, including pacman 6.1.0 itself. The tracked catalog snapshot predates that installation, so MSYS2's effective configuration for the snapshot this knowledge base composes remains uncaptured, and repair is unobserved.

**8 Toolchains — `partial`.** Fifteen per-tool pages plus the LLVM umbrella. One bounded compile-and-execute probe and one completed zlib build exist; no toolchain binary has been analysed, and the llvm-tools file list is upstream's rather than observed.

**9 Git for Windows — `partial`.** Eight pages from gitforwindows.org and the Git manual pages, now anchored by controlled local observations: launcher hashes, a byte-level PE-import parse of git.exe, and three-shell-context PATH resolution. All of it is one workstation and one PATH configuration, and Git Credential Manager's repository was unreachable, so its internals are unverified.

**10 Interactive Architecture Explorer — `partial`.** Eight pages averaging fewer than two second-level headings each — the thinnest prose density in the knowledge base. The explorer itself is built and tested, and now carries a real D3 force-directed graph view; the volume documenting it is not written to the same standard, and the graph view has not been confirmed in a live browser.

**11 Package Catalog — `partial`.** The repository-to-package inventory is directly observed across 15,711 packages and 6 repositories, the strongest observational base in the knowledge base. The package-to-file inventory beneath it covers 2, with three further archive payloads analysed separately. docs/DEEP-INVENTORY-BLOCKER.md states what the remaining nine roadmap items wait on, which statements elsewhere are qualified by it, and the command that would close it.

**12 Source Code Organization — `partial`.** Two pages. The provenance model and its collection boundary were already authored; what was missing was any measurement of the trees themselves. A 2026-08-03 pass added the measured shape from the recipe projection: 3,498 recipes across MSYS2-packages (458) and MINGW-packages (3,040) declaring 13,656 packages, 58% of recipes producing exactly four through ${MINGW_PACKAGE_PREFIX} expansion. Every figure is a floor, because the projection records a recipe only when one of its dependencies resolves. No claim ties a published archive to the recipe that names it; that needs source retrieval, patch application, and build evidence for one revision, which exists only for the bounded zlib slice.

**13 Dependency Analysis — `partial`.** One authored page over a substantial generated base: the reverse-dependency impact report exceeds a thousand lines and the dependency edges are observed. The analysis exists; the volume interpreting it does not.

**14 Build Systems — `partial`.** Two pages. Recipes are ingested statically without executing PKGBUILDs, so build mechanics are modelled from declarations rather than from builds. MSYS2's effective makepkg.conf is uncaptured.

**15 Extension and Plugin Architecture — `partial`.** One page. Every extension point cites a concrete implementation except external plugin, where none exists. The compatibility mechanism is documented and has never been exercised in this repository's history.

**16 Security — `partial`.** One page carrying the threat model and supply-chain analysis. Cites no evidence record. The signing posture it would need to assess is the same one Volume 7 records as unestablished.

**17 Performance — `inferred`.** Contains no MSYS2 timings. Controlled observations exist on real MSYS2 installations, but none of them timed anything, so hot-path costs are derived from published algorithms; the pacman transaction shape is the one measured part.

**18 Developer Guide — `inferred`.** Five pages answering select, build, debug, package, migrate, plus the repository workflow page. The repository workflow has been exercised end to end (PR #123); no command on the five MSYS2-facing pages has been executed, and each page says so.

**19 Operations Guide — `partial`.** Three pages covering upgrade, rollback, repair, migration, and refresh orchestration. Cites no evidence record; the procedures are authored rather than sourced or exercised.

**20 Reference Appendices — `partial`.** Reference appendices including terminology, requirements traceability, this ledger, and the AKB tooling benchmarks re-filed here from Volume 17, whose two same-host samples live there. The only page in the knowledge base with a verified status sits in this volume.

Generated from the pages and `model/volume-coverage.json` by `tools/build_volume_ledger.py`.
Edits between the surrounding markers are overwritten on the next build.

<!-- END GENERATED volume-metrics -->
